@model IEnumerable<FaultTrage_Test2.Designation>

@{
    ViewBag.Title = "Designation";
}

<h2>Designation </h2>

<p>
    @Html.ActionLink("Create New Designation ", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Designation1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Designation1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DesignationId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DesignationId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DesignationId })
        </td>
    </tr>
}

</table>
